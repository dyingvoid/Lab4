<Window x:Class="Lab4.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Lab4"
        xmlns:vm="clr-namespace:Lab4.ViewModels"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.DataContext>
        <vm:Reader />
    </Window.DataContext>
    <Window.Resources>
        <vm:StringFileInfoConverter x:Key="SFIConverter"/>
    </Window.Resources>
    <ScrollViewer>
        <StackPanel>
            <TextBox Text="{Binding CurrentFile, ValidatesOnExceptions=True, 
            UpdateSourceTrigger=PropertyChanged, Converter={StaticResource SFIConverter}}"/>
            <Button Content="Open file" Command="{Binding OpenFileCommand}"/>
            <Button Content="Sort file" Command="{Binding SortFileCommand}"/>
            <StackPanel>
                <StackPanel Orientation="Horizontal">
                    <DataGrid ItemsSource="{Binding CurrentBatch1.Data}"/>
                    <DataGrid ItemsSource="{Binding CurrentBatch1.Log}" />
                </StackPanel>
            </StackPanel>
            <StackPanel>
                <StackPanel Orientation="Horizontal">
                    <DataGrid ItemsSource="{Binding CurrentBatch2.Data}"/>
                    <DataGrid ItemsSource="{Binding CurrentBatch2.Log}" />
                </StackPanel>
            </StackPanel>
            <StackPanel>
                <DataGrid ItemsSource="{Binding MergedBatch.Data}"/>
                <DataGrid ItemsSource="{Binding MergedBatch.Log}"/>
            </StackPanel>
        </StackPanel>
    </ScrollViewer>
</Window>
